# spring-boot-opentelemetry-demo-24-07
This repository demonstrates the integration of OpenTelemetry with a Spring Boot application. 

## Run the application

``` bash
docker compose up
```

``` bash
./gradlew --no-daemon clean bootRun
```
